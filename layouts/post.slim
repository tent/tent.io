doctype html
html lang='en'
  head == render '_head'
  body
    == render '_navbar'
    .container
      .row
        .span6.offset3
          - if @item.identifier == "/blog/" || @item.identifier == "/blog/archive/"
            == yield
          - else
            == render '_post', post: @item, content: yield
    == render '_footer'
